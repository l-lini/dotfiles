make program shortcuts (WIN+d for discord)
programs have their own workspace and start automatically if not started
shortcut info screen (save it somwewhere globally in nix)
autotiling sway (split horizontally maybe because of large font. only on power maybe.)
    first
       |
       |    or -------
       |
    then
       |
       |--- or -------
       |          |
    then
       |
    ---|---
       |
    no more.
Bigger mouse
Git default pull/push with current branch
Git default pull with rebase
Fix terminal
Switch terminal so it not do the ssh stupidly
fuzzel (big text)
fix mouse lock
modularize sway config
Neovim system clipboard cut
Neovim force sudo (Don't warn about no write access)
Make nix lsp shut the fuck up
Make sudo shut the fuck up
Move /stay to ./keys.nix (gitignore it of course)
fix bluetooth memory
Bluetooth shortcut
fix spotify memory
Encrypted secrets
Make cli utility scripts into local package or github repo
Notifs for info stuffs
Auto wifi passwords from folder of passwords

